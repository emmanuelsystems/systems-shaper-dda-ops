# Open Decisions

1. Should `systems-shaper-dda-ops` replace or sit above `dda-agent-ops`?
2. Should `dda-agent-ops` remain the DDA pilot repo?
3. Should this repo own only workflow operations, or also agent source?
4. Who approves promotion into `main`?
5. Should David use return branches by default?
6. What counts as accepted enough to move from `runs/` into `workflows/`?
7. Should Linear / Symphony, Notion, or GitHub Issues own final project status?
8. When should workflow docs become reusable templates?
